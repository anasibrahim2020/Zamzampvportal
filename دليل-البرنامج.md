# دليل بورتال زمزم للطلبات المالية — كل ما يخص البرنامج

> بورتال طلبات الصرف والإلغاء لشركة **زمزم للحج والعمرة** (قطر).
> تطبيق ويب (PWA) يشتغل على الموبايل والآيباد والكمبيوتر، مع أرشيف سحابي وإشعارات تلقائية.

---

## 1) نظرة عامة

| البند | التفاصيل |
|------|----------|
| **الاسم** | Zamzam PV — بورتال طلبات الصرف والإلغاء |
| **النوع** | تطبيق ويب متجاوب + PWA (قابل للتثبيت على الشاشة الرئيسية) |
| **اللغة** | عربي (RTL) مع إنجليزي مساعد |
| **الواجهة** | HTML + CSS + JavaScript (بدون أطر/frameworks) |
| **الخلفية (Backend)** | Supabase (قاعدة بيانات + تسجيل دخول + تخزين ملفات + وظائف) |
| **الاستضافة** | Vercel (نشر تلقائي من GitHub) |
| **الرابط المباشر** | https://zamzampvportal.vercel.app |

---

## 2) المستخدمون والصلاحيات

الدخول عبر **اسم مستخدم + كلمة مرور** (المصادقة الفعلية على Supabase Auth — كلمات المرور مخزّنة هناك، مش في الكود).

| اسم المستخدم | الاسم | الصلاحية (role) | القسم | يقدر يعمل |
|-------------|------|------------------|------|-----------|
| `admin` | أنس إبراهيم | **accountant** (محاسب) | الإدارة المالية | يشوف كل الطلبات، يعتمد، يرفع إثبات التحويل، يعدّل |
| `ahmed_taha` | أحمد طه | **sales** (مبيعات) | قسم العمليات | ينشئ طلبات، يشوف طلبات المبيعات، يعدّل طلباته غير المعتمدة |
| `amr_mohamed` | عمرو محمد | **sales** (مبيعات) | قسم المبيعات | نفس صلاحيات المبيعات |
| `ibrahim_sabl` | إبراهيم سبل | **viewer** (عرض) | فريق الإدارة | عرض وطباعة فقط لكل الطلبات (بدون إنشاء/تعديل) |

**ملخص الأدوار:**
- **محاسب**: تحكّم كامل + اعتماد الطلبات + رفع إثبات التحويل.
- **مبيعات**: إنشاء وتعديل طلباته (طالما غير معتمدة)، ويشوف طلبات المبيعات فقط.
- **عرض (viewer)**: يشوف كل حاجة للطباعة فقط.

> لإضافة مستخدم: يتضاف في `USER_MAP` أعلى [assets/js/script-5.js](assets/js/script-5.js) **و** يتعمله حساب في Supabase Auth بنفس الإيميل، **و** (للإشعارات) يتضاف في `DIRECTORY` داخل [supabase/functions/notify/index.ts](supabase/functions/notify/index.ts).

---

## 3) الصفحات والوظائف

1. **طلب صرف (Payment Request)** — نموذج صرف: بيانات المُقدّم، فواتير المورّد، مركز التكلفة (فواتير العميل/Odoo)، إجمالي المبلغ، المرفقات، التوقيع الإلكتروني.
2. **طلب إلغاء واسترداد (Cancellation / Refund)** — نموذج إلغاء فاتورة واسترجاع مبلغ، بنود الإلغاء، تقرير الاسترجاع، حساب المستفيد (تحويل فوري/آيبان)، الشروط والأحكام.
3. **إدارة الطلبات (Requests Management)** — أرشيف كل الطلبات مع تبويبات:
   - الكل / طلبات الصرف / طلبات الإلغاء والاسترداد / المُلغاة–المسحوبة.
   - بحث + فلتر بالتاريخ، وأعمدة: رقم الطلب، النوع، التاريخ، المورّد، المبلغ، التوقيع، الاعتماد، التعليقات، إثبات التحويل، المرفقات، الإجراءات، التوقيت.
   - على الموبايل/الآيباد تتحوّل لـ**كروت**؛ على الديسكتوب **جدول**.
   - **زر المرفقات** (أيقونة المشبك) بيفتح قائمة فيها: **تنزيل الطلب PDF** مباشرةً، و**تنزيل الطلب + المرفقات** في ملف واحد، وتنزيل/طباعة/معاينة كل مرفق على حدة. الزر ظاهر حتى لو الطلب من غير مرفقات (عشان تنزيل الطلب نفسه).
   - **التحويل المجمّع** (للمحاسب): زر «تحويل مجمّع» في رأس الصفحة — راجع القسم 7-د.

---

## 4) سير العمل (Workflow)

```
مبيعات ينشئ طلب صرف ويوقّع إلكترونيًا
        ↓ (إشعار للمحاسب)
محاسب يراجع ويعتمد الطلب
        ↓ (إشعار للإدارة — إبراهيم)
تتم عملية التحويل، والمحاسب يرفع إثبات التحويل
        ↓ (إشعار لصاحب الطلب)
الطلب مكتمل — متاح للعرض والطباعة في الأرشيف
```

- الطلب المعتمد من الحسابات لا يقدر المبيعات يعدّله (طباعة فقط).
- المحاسب يقدر يلغي الاعتماد لو احتاج.

---

## 5) التقنيات وهيكل الملفات

### الملفات الأساسية
| الملف | الوظيفة |
|------|---------|
| [index.html](index.html) | الصفحة الكاملة (شاشة الدخول + كل النماذج + الأرشيف) |
| [manifest.json](manifest.json) | إعدادات PWA (الاسم، الأيقونات، الألوان، standalone) |
| [sw.js](sw.js) | Service Worker: كاش الملفات + استقبال إشعارات Push |
| [assets/css/styles.css](assets/css/styles.css) | كل التنسيقات + التصميم المتجاوب + كروت الموبايل + Safe Area |
| [assets/css/cost-center-toggle.css](assets/css/cost-center-toggle.css) | تنسيقات مركز التكلفة |
| [assets/js/script-5.js](assets/js/script-5.js) | منطق التطبيق الرئيسي (دخول، نماذج، أرشيف، توقيع، Supabase) |
| [assets/js/script-1..4.js](assets/js/) | مكتبات مساعدة (Supabase SDK، معالجة صور/PDF… إلخ) |
| [assets/js/push.js](assets/js/push.js) | تسجيل اشتراك إشعارات Push للجهاز |
| assets/fonts/ | خطوط Cairo (عربي) + Montserrat + Dancing Script |
| assets/images/ | اللوجو والأيقونات |

### مجلد Supabase
| الملف | الوظيفة |
|------|---------|
| [supabase/functions/notify/index.ts](supabase/functions/notify/index.ts) | Edge Function: الإشعارات (إيميل + واتساب + Push) |
| [supabase/schema-notify.sql](supabase/schema-notify.sql) | أعمدة الإشعارات في جدول requests |
| [supabase/schema-push.sql](supabase/schema-push.sql) | جدول اشتراكات Push |
| [supabase/schema-transfer-group.sql](supabase/schema-transfer-group.sql) | أعمدة التحويل المجمّع (إثبات واحد لعدة طلبات) |
| [supabase/NOTIFY_SETUP.md](supabase/NOTIFY_SETUP.md) | دليل إعداد إيميل/واتساب |
| [supabase/PUSH_SETUP.md](supabase/PUSH_SETUP.md) | دليل إعداد إشعارات Push |

---

## 6) قاعدة البيانات (Supabase)

- **مشروع Supabase:** `https://piwuyaskrvuhblenixhf.supabase.co`
- **جدول `requests`** — كل الطلبات. أهم الأعمدة:
  `req_no` (رقم الطلب), `doc_type` (disb/cancel), `created_by`, `department`, `amount`,
  `req_date`, `beneficiary`, `invoice_ref`, `supplier_invoices` (JSON), `attachments_data` (JSON),
  `signed_by`, `signed_at`, `accounts_signed_by`, `accounts_signed_at`,
  `transfer_image` (إثبات التحويل), `transfer_seen`, `request_pdf`, `cancelled`, `created_at`.
  أعمدة **التحويل المجمّع**: `transfer_group` (رقم المجموعة مثل `TRF-0001`), `transfer_group_at` (توقيت التجميع), `transfer_group_note` (مرجع/ملاحظة التحويل).
- **جدول `push_subscriptions`** — أجهزة مشتركة في الإشعارات: `user_name`, `endpoint`, `p256dh`, `auth`.
- **التخزين (Storage):** bucket باسم `request-attachments` للفواتير وإثبات التحويل.
- **RLS:** مفعّل؛ المستخدم المسجّل يقدر يقرأ/يكتب طلباته حسب صلاحيته.

> ملفات SQL للتشغيل مرة واحدة: `schema-notify.sql` و `schema-push.sql` و **`schema-transfer-group.sql`**.

---

## 7) الإشعارات

### أ) داخل البورتال
لما صاحب الطلب يفتح البورتال وطلبه اتحوّل، بيظهرله تنبيه (تتبّع عبر عمود `transfer_seen`).

### ب) تلقائية (عبر Database Webhook → Edge Function `notify`)
ثلاثة أحداث على جدول `requests`، وكل حدث يوصل لناس معيّنين على 3 قنوات:

| الحدث | المُبلَّغ | القنوات |
|------|----------|---------|
| طلب صرف جديد (INSERT disb) | المحاسب | إيميل + واتساب + **Push** |
| رفع إثبات التحويل (transfer_image) | صاحب الطلب | إيميل + واتساب + **Push** |
| اعتماد المحاسب (accounts_signed_by) | الإدارة (إبراهيم) | إيميل + **Push** |

**القنوات:**
- **إيميل:** Gmail SMTP (`portal.zamzam@gmail.com` بـ App Password) — تصميم بهوية زمزم.
- **واتساب:** CallMeBot (كل مستخدم له `apikey` بعد تفعيله على موبايله).
- **Push (Web Push):** إشعار على الموبايل حتى والتطبيق مقفول، باللوجو — عبر مفاتيح VAPID.

### ج) إشعارات Push — ملاحظات مهمة
- **أندرويد:** يشتغل من كروم بعد الموافقة على الإذن.
- **آيفون/آيباد:** لازم iOS **16.4+** و**تثبيت التطبيق على الشاشة الرئيسية** وفتحه من الأيقونة (مش من سفاري).
- أول دخول بيظهر زر «🔔 تفعيل الإشعارات» — يضغطه المستخدم ويوافق.
- التفاصيل الكاملة في [supabase/PUSH_SETUP.md](supabase/PUSH_SETUP.md).

### د) التحويل المجمّع (إثبات تحويل واحد لعدة طلبات)

لما المحاسب يعمل **تحويل بنكي واحد يغطّي أكتر من طلب**، يقدر يربطهم كمجموعة بإثبات واحد:

1. من **إدارة الطلبات** → زر **«تحويل مجمّع»** (يظهر للمحاسب فقط) → الجدول يدخل **وضع التحديد**.
2. مربعات التحديد بتظهر في عمود «إثبات التحويل» للطلبات **المعتمدة وغير الملغاة واللي لسه مرفعلهاش إثبات**.
3. شريط سفلي بيعرض **عدد الطلبات المحددة + إجمالي المبلغ**، مع «تحديد كل المعروض» و«إنهاء».
4. اضغط **«رفع إثبات تحويل مجمّع»** → نافذة بتعرض قائمة الطلبات وإجماليها + خانة **مرجع التحويل (اختياري)** + اختيار ملف الإثبات (صورة/PDF — حد أقصى 2 ميجا).
5. بعد الرفع: كل الطلبات بتاخد **نفس الإثبات** و**نفس رقم المجموعة** (`TRF-0001`, `TRF-0002` … بالتسلسل).

**بعد التجميع:**
- كل طلب في المجموعة بيظهر بـ **شارة `TRF-XXXX`** تحت رقم الطلب وشريط جانبي بلون المجموعة.
- عمود «إثبات التحويل» بيبقى أيقونة طبقات + **عدد طلبات المجموعة**، والقائمة بتعرض: معاينة/تنزيل الإثبات، عدد الطلبات، إجمالي المجموعة، مرجع التحويل، و**عرض طلبات المجموعة** (بيفلتر القائمة على المجموعة ويعرض لافتة بالإجمالي).
- تقدر تبحث برقم المجموعة (`TRF-0001`) في خانة البحث العادية.
- **الحذف (للمحاسب):** «حذف الإثبات من هذا الطلب» (يخرجه من المجموعة بس) أو «حذف الإثبات من كل المجموعة» (يفك المجموعة كلها).
- **الإشعارات:** كل صاحب طلب في المجموعة بياخد إشعاره عادي (إيميل/واتساب/Push) — يعني لو حد عنده 3 طلبات في نفس المجموعة هيوصله 3 إشعارات.

> الميزة محتاجة تشغيل [supabase/schema-transfer-group.sql](supabase/schema-transfer-group.sql) مرة واحدة. لو الأعمدة مش موجودة، الزر بيوضّح الـ SQL المطلوب.

---

## 8) الأسرار (Supabase → Edge Functions → Secrets)

| المفتاح | الغرض |
|--------|-------|
| `GMAIL_USER` | إيميل المُرسِل |
| `GMAIL_APP_PASSWORD` | App Password للإيميل |
| `VAPID_PUBLIC` | مفتاح Push العام (نفس اللي في push.js) |
| `VAPID_PRIVATE` | مفتاح Push الخاص (**سري** — ماينفعش في GitHub) |
| `VAPID_SUBJECT` | `mailto:portal.zamzam@gmail.com` |
| (تلقائي) | `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` |

> المفتاح العام VAPID:
> `BPc-3luAluz5vEWzeUKFX2gjwYbpuMKjCQgTLdn9tsRLG34pREWZCitsSTXLGFwvR8Xtm6_gCD1NunG90gCFKAA`

---

## 9) PWA والكاش (Service Worker)

- التطبيق قابل للتثبيت على الشاشة الرئيسية (standalone).
- `sw.js` بيكاش ملفات الواجهة عشان الفتح السريع + يستقبل Push.
- **رقم نسخة الكاش** (`CACHE = 'zamzam-vXX'`) في أول [sw.js](sw.js): **كل ما تعدّل CSS/JS لازم ترفع الرقم** عشان الأجهزة تجيب النسخة الجديدة وتمسح القديمة.
- النسخة الحالية: **v17**.

---

## 10) التصميم المتجاوب (Responsive)

| العرض | السلوك |
|------|--------|
| ≤ 640px (موبايل) | عمود واحد؛ إدارة الطلبات = كروت في عمود |
| 641–1024px (آيباد/تابلت) | نماذج بأعمدة؛ الكروت في عمودين/ثلاثة |
| > 1024px (ديسكتوب) | النماذج كاملة؛ إدارة الطلبات = جدول |

- تم ضبط **Safe Area** لـ iOS عشان البار العلوي يملا منطقة الـstatus bar (بدون فراغ أبيض فوق).
- خط Cairo تم تثبيته على `slnt=0` (كان بيظهر مايل على iOS).

---

## 11) النشر والتحديث

1. عدّل الملفات محليًا.
2. **لو غيّرت CSS/JS:** ارفع رقم `CACHE` في [sw.js](sw.js).
3. ارفع الملفات على **GitHub** (Add file → Upload files → Commit).
4. **Vercel** بيعمل Deploy تلقائي.
5. للـEdge Function/SQL/الأسرار: من **Supabase Dashboard** مباشرة.
6. على الأجهزة: اقفل التطبيق وافتحه (أو احذف وأعد التثبيت لو التغيير في manifest/status bar).

---

## 12) سجل التعديلات الأخيرة

**نسخة v17:**
- **تنزيل الطلب PDF مباشرة** من قائمة المرفقات في إدارة الطلبات (وكمان «الطلب + المرفقات» في ملف واحد)، والزر بقى ظاهر حتى للطلبات بدون مرفقات.
- تصغير حجم ملف الـPDF المُنزّل (التقاط JPEG بدل PNG) — من ~30 ميجا إلى أقل من 1 ميجا.
- **التحويل المجمّع:** تحديد أكتر من طلب وربطهم بإثبات تحويل واحد ورقم مجموعة `TRF-XXXX`، مع شارة ولون للمجموعة، وفلترة وعرض إجمالي المجموعة، وحذف الإثبات من طلب واحد أو من المجموعة كلها.

**قبل كده:**
- إصلاح الخط العربي المايل على iOS (تثبيت محور الميلان في Cairo).
- تحويل جدول «إدارة الطلبات» لكروت على الموبايل والآيباد.
- تصميم متجاوب كامل لكل الصفحات (320px → 1440px).
- إصلاح منطقة الـstatus bar على الآيباد (Safe Area + black-translucent).
- إضافة إشعارات **Web Push** (تظهر والتطبيق مقفول، باللوجو) على الأحداث الـ3.

---

## 13) قيود معروفة

- إشعارات Push على الآيفون تحتاج تثبيت التطبيق على الشاشة الرئيسية (قيود Apple).
- كلمات مرور الدخول تُدار من Supabase Auth (مش من داخل الكود).
- CallMeBot للواتساب مجاني ويحتاج تفعيل يدوي لكل رقم.

---

*آخر تحديث لهذا الدليل: مع نسخة الكاش v17.*
